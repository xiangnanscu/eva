{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy":"git add . && git commit -am x && git push -o echoback prod master",
    "echoback":"prisma db push",
    "push":"git add . && git commit -am x && git push origin master",
    "db:init": "npx prisma migrate dev --name init",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/node": "^18.7.18",
    "nuxt": "3.0.0-rc.11",
    "prisma": "^4.3.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3"
  },
  "dependencies": {
    "@prisma/client": "^4.3.1"
  }
}
